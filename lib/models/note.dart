class Note {
  final int id;
  String title;
  String content;
  DateTime createdTime;

  Note({
    required this.id,
    required this.title,
    required this.content,
    required this.createdTime,
  });
}
